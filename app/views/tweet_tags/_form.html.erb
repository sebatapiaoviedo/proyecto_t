<%= form_with(model: tweet_tag, local: true) do |form| %>
  <% if tweet_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet_tag.errors.count, "error") %> prohibited this tweet_tag from being saved:</h2>

      <ul>
        <% tweet_tag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tweet_id %>
    <%= form.text_field :tweet_id %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.text_field :tag_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
